const CreateSmartContract = () => {
    return (
        <div>
            <p className="enableMetaMask" onClick={createSmar}>
                Create a smart contract
            </p>
        </div>
    );
};
